import styled from 'styled-components';

export const StyledProfilePopupModule = styled.div`
  position: relative;
`;

export const StyledUsername = styled.span`
  color: #fff;
  cursor: pointer;

  &:hover {
    text-decoration: underline;
  }
`;
