import { StyledScheduleTemplate } from './ScheduleTemplate.styled';

const ScheduleTemplate: React.FC = () => {
  return <StyledScheduleTemplate></StyledScheduleTemplate>;
};

export default ScheduleTemplate;
